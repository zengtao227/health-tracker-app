# Android
.gradle/
build/
app/build/
local.properties
*.apk
*.aar
*.jks

# IDE - IntelliJ / Android Studio
.idea/
*.iml
*.iws
*.ipr
out/

# iOS / macOS
Healthtracker/DerivedData/
Healthtracker/build/
Healthtracker/Healthtracker.xcodeproj/project.xcworkspace/
Healthtracker/Healthtracker.xcodeproj/xcuserdata/
*.xcresult
*.xcuserstate

# OS
.DS_Store
Thumbs.db

# Release
releases/*.apk
releases/*.zip
# (We keep releases folder but normally we ignore binaries, 
# but since the user specifically asked to put them there, we allow them)
!releases/HealthTracker-v1.0.apk
!releases/HealthTracker-macOS.zip
